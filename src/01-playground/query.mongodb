use('sample_training')

/**
 * db -> elemnto de bd
 * zip -> bd a consultar
 * .find() -> accion  / {llave :valor}
 */
db.zips.find({state: "NY"})

//Para contar
//db.zips.find({state: "NY"}).count()

db.zips.findOne({_id: ObjectId(  "5c8eccc1caa187d17ca72f8f") })
